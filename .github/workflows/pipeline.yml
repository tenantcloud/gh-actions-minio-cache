name: Pipeline

on:
  pull_request:
    branches: [ master ]
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: pipeline
    container:
      image: node:20
    steps:
      - name: Checkout 👀
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build
        run: |
            yarn install
            yarn ts-jest -v

